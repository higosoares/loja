{% extends 'base.html.twig' %}

{% block title %}TipoPerfil{% endblock %}

{% block body %}
    <h1>TipoPerfil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdTipoPerfil</th>
                <td>{{ tipo_perfil.idTipoPerfil }}</td>
            </tr>
            <tr>
                <th>TxNomeTipoPerfil</th>
                <td>{{ tipo_perfil.txNomeTipoPerfil }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_perfil_index') }}">back to list</a>

    <a href="{{ path('tipo_perfil_edit', {'idTipoPerfil': tipo_perfil.idTipoPerfil}) }}">edit</a>

    {{ include('tipo_perfil/_delete_form.html.twig') }}
{% endblock %}
